<h1>Questions</h1>

<table>
  <tr>
    <th>Title</th>
  </tr>

  <%= link_to 'Ask a question', new_question_path %>

  <% @question.each do |q| %>
  <tr>
    <td><a href="/questions/<%= q.id %>"><%= q.title %></a></td>
  </tr>
  <% end %>
</table>
